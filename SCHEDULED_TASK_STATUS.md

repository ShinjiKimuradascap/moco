# スケジュールタスクの状況と調査結果

## 問題
スケジュールタスクがWhatsAppにメッセージを送ってこない。

## 調査結果

### 1. スケジュールタスクの状態
`tasks.db` に以下のタスクが登録されている:
- `test_schedule_python`: 現在時刻を教えて (*/1 * * * *) - 毎分実行
- `test_ping`: 現在時刻を教えて (*/1 * * * *) - 毎分実行
- `test_scheduler_check`: テスト: 現在時刻をWhatsAppに送信してください (*/1 * * * *) - 毎分実行
- `slack-summary-sg-time`: Slackチャンネルの要約 (0 8,12,17 * * *) - 毎日8:00, 12:00, 17:00
- `test-slack-summary-v3-fixed`: Slack履歴要約 (58 11 * * *) - 毎日11:58
- `task_e08e2081` (daily-news-summary): ニュースサマリー (0 8 * * *) - 毎日8:00

### 2. スケジューラーの状態
- `MocoScheduler` クラスが存在する (`src/moco/core/scheduler.py`)
- スケジューラーは API モードで動作（API 経由でタスクを実行）
- `start_scheduler.py` でスケジューラーを起動したが、タスクが実行されない

### 3. APIの状態
- `moco ui` がポート8000で実行中 (PID 79304)
- `/api/chat` エンドポイントは正常に動作
- 手動で curl でリクエストを送ると正常に応答する

### 4. 問題の原因
**スケジューラーが起動していない、または起動してもタスクを実行しない**

## 原因分析

### 可能性1: API URLの問題
- `MocoScheduler` は `MOCO_API_URL` 環境変数またはデフォルト値を使用
- デフォルトは `http://localhost:8000/api/chat`
- これが正しく設定されているか確認が必要

### 可能性2: スケジューラーのコールバックが実行されない
- スケジューラーには `after_execute_callback` があるが、これはタスク完了後にのみ呼ばれる
- タスク自体が実行されない場合、コールバックも呼ばれない

### 可能性3: スケジューラーが実行されない
- `start_scheduler.py` を実行したが、タスクが実行されない
- ログが出力されていないため、スケジューラーが正常に動作していない可能性

## 次のステップ

1. スケジューラーをデバッグモードで実行してログを確認
2. `debug_scheduler.py` を使用して詳細なログを出力
3. WhatsApp 通知が送信されるか確認
