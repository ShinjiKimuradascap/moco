# スケジュールタスクの調査結果

## 問題
スケジュールタスクがWhatsAppにメッセージを送ってこない。

## 調査結果

### 1. スケジュールタスクの状態
`tasks.db` に以下のタスクが登録されている:
- `test_schedule_python`: 現在時刻を教えて (*/1 * * * *) - 毎分実行
- `test_ping`: 現在時刻を教えて (*/1 * * * *) - 毎分実行
- `test_scheduler_check`: テスト: 現在時刻をWhatsAppに送信してください (*/1 * * * *) - 毎分実行
- `slack-summary-sg-time`: Slackチャンネルの要約 (0 8,12,17 * * *) - 毎日8:00, 12:00, 17:00
- `test-slack-summary-v3-fixed`: Slack履歴要約 (58 11 * * *) - 毎日11:58
- `task_e08e2081` (daily-news-summary): ニュースサマリー (0 8 * * *) - 毎日8:00

### 2. スケジューラーの状態
- `MocoScheduler` クラスが存在する (`src/moco/core/scheduler.py`)
- スケジューラーは API モードで動作（API 経由でタスクを実行）
- `debug_scheduler.py` を実行した結果、スケジューラーは正常に動作している
- `test_ping` タスクが実行され、最終実行時刻が更新された (last_run: 2026-01-27T14:28:14.850352)

### 3. APIの状態
- `moco ui` がポート8000で実行中 (PID 79304)
- `/api/chat` エンドポイントは正常に動作
- 手動で curl でリクエストを送ると正常に応答する

### 4. WhatsApp データベースの確認
`moco_whatsapp` は whatsmeow (WhatsApp ライブラリ) の SQLite データベース:
- チャット設定、連絡先、セッションなどの情報が含まれている
- 送信されたメッセージが含まれている可能性がある

## 結論

スケジューラーは正常に動作しており、タスクが実行されています。ただし、WhatsApp通知が送信されているかどうかは確認が必要です。

## 次のステップ

1. WhatsApp 通知が送信されたか確認:
   - WhatsApp アプリを開いてメッセージを確認
   - `moco_whatsapp` データベースにメッセージが含まれているか確認

2. WhatsApp 通知が送信されない場合:
   - `NotifyMobileTool` の実装を確認 (`src/moco/tools/mobile.py`)
   - WhatsApp チャンネルの接続状態を確認
   - エラーログを確認

3. スケジューラーをバックグラウンドで実行:
   - `debug_scheduler.py` をバックグラウンドで実行
   - 定期的にタスクが実行されることを確認
